<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>ProxyFlow Admin | Dashboard</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #0f0f0f; color: #e0e0e0; padding: 20px; }
        .stats-container { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-bottom: 30px; }
        .card { background: #1a1a1a; padding: 20px; border-radius: 12px; border: 1px solid #333; }
        .card h3 { margin: 0; color: #888; font-size: 14px; text-transform: uppercase; }
        .card p { font-size: 28px; margin: 10px 0; color: #fff; font-weight: bold; }
        .section { background: #1a1a1a; padding: 20px; border-radius: 12px; border: 1px solid #333; margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th { text-align: left; padding: 12px; border-bottom: 2px solid #333; color: #888; }
        td { padding: 12px; border-bottom: 1px solid #222; }
        input { background: #222; border: 1px solid #444; color: white; padding: 8px; border-radius: 4px; margin-right: 5px; }
        .btn { padding: 8px 15px; border-radius: 5px; cursor: pointer; border: none; font-weight: bold; }
        .btn-green { background: #2ecc71; color: white; }
        .btn-red { background: #e74c3c; color: white; }
        .btn-blue { background: #3498db; color: white; }
        .badge { background: #f3ba2f; color: black; padding: 3px 7px; border-radius: 4px; font-size: 11px; font-weight: bold; }
    </style>
</head>
<body>

    <h1 style="color: #fff;">📊 ProxyFlow Control Center</h1>

    <div class="stats-container">
        <div class="card" style="border-top: 4px solid #3498db;">
            <h3>👥 Total Utilisateurs</h3>
            <p><%= stats.totalUsers %></p>
        </div>
        <div class="card" style="border-top: 4px solid #2ecc71;">
            <h3>🛒 Proxys Vendus</h3>
            <p><%= stats.totalSold %></p>
        </div>
        <div class="card" style="border-top: 4px solid #f1c40f;">
            <h3>💰 Gains Estimes</h3>
            <p><%= stats.totalEarnings %> $</p>
        </div>
    </div>

    <div class="section">
        <h2>📦 Commandes en attente</h2>
        <table>
            <thead>
                <tr>
                    <th>Méthode</th>
                    <th>ID Paiement</th>
                    <th>Action (Livrer Proxy)</th>
                </tr>
            </thead>
            <tbody>
                <% pendingOrders.forEach(order => { %>
                    <tr>
                        <td><span class="badge"><%= order.method %></span></td>
                        <td><b style="color: #f1c40f;"><%= order.paymentRef %></b></td>
                        <td>
                            <form action="/admin/deliver" method="POST" style="display: inline-flex;">
                                <input type="hidden" name="orderId" value="<%= order.orderId %>">
                                <input type="text" name="proxyData" placeholder="IP:PORT:USER:PASS" required>
                                <button type="submit" class="btn btn-green">Valider</button>
                            </form>
                            <form action="/admin/reject" method="POST" style="display: inline;">
                                <input type="hidden" name="orderId" value="<%= order.orderId %>">
                                <button type="submit" class="btn btn-red">X</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <div class="section">
        <h2>👥 Liste des Utilisateurs</h2>
        <table>
            <thead>
                <tr>
                    <th>Email</th>
                    <th>Solde</th>
                    <th>Ajouter Argent</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(user => { %>
                    <tr>
                        <td><%= user.email %></td>
                        <td style="color: #2ecc71; font-weight: bold;"><%= user.balance %> $</td>
                        <td>
                            <form action="/admin/add-balance" method="POST">
                                <input type="hidden" name="psid" value="<%= user.psid %>">
                                <input type="number" name="amount" placeholder="0.00" step="0.01" required style="width: 60px;">
                                <button type="submit" class="btn btn-blue">Créditer</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

</body>
</html>
